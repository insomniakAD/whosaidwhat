# Rust
target/
Cargo.lock

# Python
__pycache__/
*.pyc
venv/
.venv/
*.egg-info/

# macOS
.DS_Store

# Node / Tauri
# NOTE: /dist is NOT ignored — the frontend is hand-written static HTML/CSS/JS
# (no bundler), so dist/ is source, not build output.
node_modules/
src-tauri/gen/

# Local artifacts
*.sqlite
*.sqlite-wal
*.sqlite-shm
recordings/
last_processed.txt
meeting_blog_post.md
logs/*.log
